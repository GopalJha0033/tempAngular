export interface Task {
    id: string,
    title: string,
    dueDate: string,
    summary: string,
    userId: string
}

export interface NewTask {
    id: string,
    title: string,
    dueDate: string,
    summary: string,
    userId: string
}